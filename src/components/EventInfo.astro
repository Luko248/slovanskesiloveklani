---
import { useTranslations } from '../i18n/utils';

interface Props {
  lang: 'cs' | 'en';
}

const { lang } = Astro.props;
const t = useTranslations(lang);

// TODO: Move this content to i18n
const features = [
  "Originální disciplíny / Original disciplines",
  "Komplexní zatížení / Full body challenge",
  "Zdravotník na místě / Medic on site",
  "Občerstvení / Refreshments",
  "Soutěžní trička / Competition T-shirts",
  "Adrenalin & Soudržnost / Adrenaline & Camaraderie"
];
---

<section id="about" class="py-20 bg-parchment dark:bg-deep-charcoal relative">
    <div class="container mx-auto px-4">
        <div class="grid md:grid-cols-2 gap-12 items-center">
            
            <!-- Text Content -->
            <div class="space-y-6">
                <div class="inline-flex items-center gap-3">
                    <span class="h-px w-8 bg-viking-red dark:bg-fire-orange"></span>
                    <h2 class="text-3xl md:text-4xl font-display font-bold text-viking-red dark:text-fire-orange">
                        {t('about.heading') || "O soutěži"}
                    </h2>
                    <span class="h-px w-8 bg-viking-red dark:bg-fire-orange"></span>
                </div>
                
                <p class="text-lg leading-relaxed text-charcoal dark:text-gray-300">
                    Slovanské Silové Klání je amatérská silová soutěž zaměřená na komplexní sílu celého těla. 
                    Soutěž je určena pro běžné cvičence, kteří chtějí ukázat svou funkční sílu v reálných podmínkách.
                    <br><br>
                    Na rozdíl od klasických posilovacích soutěží se zde nezvedají dokonale vyvážené činky, 
                    ale pouze to, co nám nadělila Matka Příroda.
                </p>

                <div class="grid grid-cols-1 sm:grid-cols-2 gap-3 mt-6">
                    {features.map(feature => (
                        <div class="flex items-center gap-2">
                             <svg class="w-5 h-5 text-viking-red dark:text-fire-orange flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>
                             <span class="font-semibold text-sm">{feature}</span>
                        </div>
                    ))}
                </div>
            </div>

            <!-- Visual/Image Side -->
            <div class="relative">
                <div class="absolute inset-0 border-2 border-bronze dark:border-stone transform translate-x-4 translate-y-4 rounded-lg"></div>
                <!-- Placeholder Image -->
                <div class="relative rounded-lg overflow-hidden shadow-2xl aspect-[4/3] bg-gray-300 dark:bg-slate">
                     <div class="w-full h-full bg-cover bg-center" style="background-image: url('https://images.unsplash.com/photo-1517836357463-d25dfeac3438?q=80&w=2670&auto=format&fit=crop');"></div>
                </div>
            </div>
        
        </div>
    </div>
</section>
